VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Clé pour l'analyse de CV (Gemini via CV2DOC)
AI_API_KEY=your_gemini_key_here

# Clé pour le parsing de texte (OpenAI)
OPENAI_API_KEY=your_openai_key_here

# Génération DOCX :
# - En Docker avec n8n : décommenter la ligne ci-dessous (backend appelle le webhook n8n).
# - En local sans n8n : laisser commenté (backend appelle FastAPI directement).
# N8N_WEBHOOK_URL_DOCX=http://fortil-n8n:5678/webhook/cv2doc-docx

# URL de l’interface n8n (optionnel) : utilisée par le frontend pour le lien « Ouvrir n8n » en cas d’erreur workflow.
# VITE_N8N_URL=http://localhost:5678
